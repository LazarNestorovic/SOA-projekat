Observability service (Python)

This small FastAPI app emits traces, metrics and structured logs to help verify observability pipeline.

Run (with Docker compose in repo root):

docker-compose -f docker-compose.monitoring.yml up --build
